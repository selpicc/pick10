# 이어서 작업하기 — 진행 상황 정리 (새 컴퓨터 인수인계용)

> 이 문서는 컴퓨터를 옮긴 뒤에도 작업을 그대로 이어가기 위한 "현재 상황 요약"입니다.
> 새 컴퓨터의 Claude(코워크)는 이 문서 + `CLAUDE.md`를 먼저 읽고 이어서 진행하세요.
> 사용자(밍)는 개발 초보 → 항상 쉽게 설명, 진행 전에 물어보기, 오류는 보고하고 재발 방지.

## 이 프로젝트가 하는 일 (한 줄)
영유아·임산부 브랜드를 자동 수집 → 영업 리드로 정리(Supabase DB) → 대시보드 표시
→ 브랜드별 맞춤 영업메일을 **Gmail 초안(임시보관함)** 으로 자동 생성. (발송은 사람이 직접)

## 지금까지 완성된 것 (작동 확인됨)
- **수집 엔진** (`collect_5.py` + `business_info_collector.py`): 키워드·카테고리·자동 수집.
  연락처·이메일은 **공식 홈페이지에 표기된 것만** 수집(없으면 빈칸). 속도 3.7배 개선 완료.
- **DB**: Supabase `sellers` 테이블. 수집하면 자동 저장 → 대시보드 자동 반영.
- **대시보드** (`dashboard.py`): 브랜드 목록. 행을 클릭하면 열리는 **셀러 디테일**에서
  '이 브랜드 메일 초안 만들기' 버튼으로 초안 생성. 로컬(`localhost:8501`) + 클라우드(`pick10-mingg.streamlit.app`).
- **영업메일 자동 생성** (`메일초안_생성.py` + `email_templates.py` + `brand_intro.py`):
  제품형/서비스형 2템플릿(서비스형은 지역형↔전국형 자동 분기), 조사(을/를) 자동 처리,
  **AI가 브랜드 홈페이지를 읽고 도입부 1~2줄 맞춤 작성**(실패 시 카테고리 문구로 자동 폴백).
  `--brands "A,B"` 로 방금 수집한 N개만 지정 가능. `--no-ai` 로 AI 도입부 끄기.
  ⚠️ 메일 문구 원칙은 `클로드_기억_백업.md` 15번 항목을 반드시 먼저 읽을 것
  (가격 미노출 / 소개서에 없는 성과 수치 지어내지 않기 / 레퍼런스는 과거형).
- **Gmail 연동**: `token.json` 발급 완료 → 초안 자동 생성 환경 구축.
- **Claude Code 자동화**: "튼살크림 5개 수집하고 메일 초안 만들어줘" 같은 한국어 명령 →
  수집→DB→대시보드→Gmail 초안까지 한 번에. (실행 매뉴얼은 `CLAUDE.md` 참고)

## 다음에 할 일 (우선순위 순)
1. **회사소개서 자동 첨부** — 초안에 회사소개서 PDF까지 자동으로 붙이기. (발송 버튼은 사람이)
2. **⭐ 메일 송신 후 카카오톡 알림** — 담당자가 메일을 보내면 결과를 카톡으로 보고받는 기능. (사내 AI 미팅에서 "다음 단계"로 공유함)
3. **견적요청 버튼/폼** — 영업메일에 "견적요청" 버튼 → 견적 페이지 → 요청 내용이 메일로 회신되는 흐름. (타사 광고메일 참고, 검토 중)
4. **프라젠트라 수집 보완** — 이메일(jwpark@plagentra.kr) 있는데 "프라젠트라 공식 스토어"라는 스마트스토어 접미사 때문에 공식몰을 못 잡던 케이스. (앙덤스토어와 같은 종류 문제 — 관련 수정은 일부 반영됨, 재확인 필요)
5. **셀픽 소개서 자동 첨부** — 지금 메일 본문이 "셀픽 소개서를 함께 첨부드립니다"라고 쓰므로
   **첨부를 빠뜨리면 안 된다.** 현재는 사람이 Gmail 임시보관함에서 직접 첨부. 자동화하면 실수 방지.

## 환경 메모 (2026-07)
- 커밋·푸시는 **selpicc 계정**으로. (GCM에 ondayon2-wq 계정이 같이 저장돼 있으면 그게 먼저
  잡혀 403이 남 → `git-credential-manager erase` 로 지우면 selpicc로 붙음)
- `.env` 에 `GEMINI_API_KEY` 추가됨 (AI 도입부 생성용). 절대 커밋 X.
- `self_check.py` 는 10개 항목 — 코드 고친 뒤 항상 실행.

## 꼭 지킬 원칙
- 메일은 **초안까지만** 자동. 첨부·발송 버튼은 사람이 누름 (실수 발송 방지).
- 연락처는 **공식 홈페이지 표기분만** 수집. 뉴스/마켓/허브몰에서 가져오지 않음.
- **커밋·푸시는 사용자 확인 후.** 커밋 문구는 제안만.
- `.env`, `token.json`, `credentials.json` 은 절대 커밋 X (비밀 파일).

## 새 컴퓨터 환경 메모
- 새 프로젝트 폴더 경로: `C:\Users\DS006\Documents\Claude\Projects\셀픽 영업처 수집\셀픽 영업처 수집`
  (폴더 이름이 두 번 겹쳐 있음 — 실제 파일은 안쪽 폴더에 있음)
- Python·Node.js·venv·Playwright·클로드 코드 설치 완료. `self_check.py` 9/9 통과 확인.
- 파이썬 실행은 항상 `venv\Scripts\python ...`

## 참고: 주요 파일
- `collect_5.py` / `business_info_collector.py` — 수집
- `메일초안_생성.py` / `email_templates.py` / `gmail_drafts.py` — 영업메일 초안
- `dashboard.py` — 대시보드
- `self_check.py` — 코드 점검 (수정 후 항상 실행)
- `진단_수집측정.py` — 수집 속도·품질 진단
- `CLAUDE.md` — 자연어 명령 → 실행 매뉴얼 (자세한 명령은 여기)
