# 클로드 기억(메모리) 백업 — 새 컴퓨터로 이어가기용

> 이전 컴퓨터의 코워크에 저장돼 있던 Claude 메모리 요약입니다.
> **새 컴퓨터의 Claude에게 이렇게 말하세요:** "이 문서를 읽고 네 메모리에 저장해줘."
> 그러면 프로젝트 규칙·수집 원칙을 새 컴퓨터에서도 똑같이 기억하고 일합니다.

## 사용자(밍) 정보
- 이메일: ondayon2@gmail.com
- 개발 초보 → 항상 쉽게 설명, 진행 전에 물어보기, 오류는 보고하고 재발 방지, 코드는 전체 흐름 보고 수정.

## 작업 방식 피드백 (사용자가 강하게 요청한 것들)
1. **코드 보내기 전 사전 점검 필수** — 같은 오류 반복을 매우 싫어함. 보내기 전 deprecated API·dtype·캐시·한글환경·컬럼명 일치 등 점검.
2. **스키마/값 변경 시 legacy 데이터 호환 필수** — 분류값·카테고리·단계명을 바꾸면 옛 데이터도 매핑해야 함. 필터 기본값은 실제 데이터의 unique 값으로 동적 설정.
3. **데이터 정규화는 저장·로드 양쪽에 동일하게** — strip/lower/normalize를 한쪽만 하면 중복 검출이 조용히 실패함.
4. **사용자 데이터 침묵 자동 삭제 금지** — 사용자 액션 시 필터·검사 결과로 몰래 삭제하지 말 것. 명시적 확인 없이 삭제하면 데이터 손실처럼 느껴짐.
5. **의미 없는 split 토큰을 매칭 풀에 넣지 말 것** — 띄어쓰기 변형의 조각 토큰("신생", "아로션" 등)을 넣으면 엉뚱한 매칭 양산. 토큰 분리는 원본 키워드만.

## 수집 엔진 관련 기억 (project)
6. **스마트스토어 URL 3중 Fallback** — redirect 실패 시 검색페이지로 가지 말고 API 원본 link(상품페이지)를 보존.
7. **SPA 공식몰 Playwright 렌더링** — JS로 그려지는 사이트(kokofit 등)는 requests로 footer를 못 읽어 엉뚱한 사이트를 수집함. 브라우저 렌더링 fallback 사용.
8. **공식 footer의 무료메일 살리기** — @naver/@gmail도 사업자번호 박힌 공식 footer면 영업 이메일로 인정. 단 stibee 등 뉴스레터 SaaS는 차단.
9. **영업정보 신뢰도 게이트(brand_score)** — 사업자번호 + 순수 브랜드 일치를 통과해야 자동수집, 아니면 '수기 입력 필요'로 남김.
10. **잡동사니 몰 필터** — 만물상/종합몰은 이름 휴리스틱 + 카탈로그 타깃 집중도로 2단 컷.
11. **마케팅 단계 검색노이즈 보정** — 브랜드명이 일반어라 검색건수가 부풀려지면 신생 브랜드가 확장기로 오분류됨 → product_count 적으면 도입기로 보정. 연락처·대표·사업자번호는 공식사이트에서만 수집.
12. **수집 속도 개선(3분→30초대)** — 조기중단 조건을 사업자번호+전화+이메일로 변경 + 뉴스사이트 후보 필터. 정확도 유지하며 3.7배 빨라짐. 측정은 `진단_수집측정.py`.
13. **키워드 검색·매칭 균형(0건 버그)** — 검색은 넓게 확장하는데 매칭이 좁으면 진짜 임산부 브랜드도 0%로 거절됨. 매칭 풀에 확장키워드 구절 추가(단, 단독 generic 토큰 산모/크림 등은 제외). 튼살크림 0건→3건.

## 셀픽 영업메일 모델 (project) — 매우 중요
14. **셀픽 = 산후조리원 키오스크 / 10만 신생아 DB 기반 마케팅** (라이브커머스 아님).
    - 서명: 셀픽이앤에스 / 영업팀 / 서민지 선임 매니저 / 010-2421-6905 / selpic100@gmail.com / selpic.kr
    - Gmail은 **초안까지만** 생성. 첨부·발송은 사람이 직접.

15. **영업메일 원칙 (2026-07 전면 개편)** — 콜드메일 회신률 중심으로 다시 씀.
    - 근거 수치는 **셀픽 MEDIA PROPOSAL(2026.07)에 실제로 있는 값만**: 조리원 230곳(M/S 50%),
      산부인과 55곳, 체류 14일 매일 접촉, 1인 15장 무료인화, 키오스크 월 300만 노출 보장,
      매월 신규 산모 DB 5,100건. **성과 수치(전환율 등)는 소개서에 없으므로 절대 지어내지 말 것.**
    - 레퍼런스: 네슬레 일루맘클럽·리안 드림콧·퓨어락 — **과거 집행 사실만** (현재형 X, 지금은 집행 중 아님).
    - **가격은 첫 메일에 넣지 않는다** (금액이 거절 트리거). 견적은 회신·미팅에서.
    - 제목에 회사명·'제안' 안 씀. 첫 문단은 우리 소개가 아니라 상대 브랜드 얘기.
    - CTA는 하나: 소개서 첨부 + 유선/미팅 요청("편하신 시간만 한 줄 회신").
    - 템플릿 2종: **제품형**(카테고리별 샘플링·LMS) / **서비스형**(지역형 ↔ 전국형 자동 분기).
      지역 판별은 전화 지역번호·주소·상호로만. 근거 없으면 **지역명을 지어내지 말고** 전국형.
    - **브랜드별 도입부는 AI(Gemini)가 홈페이지를 읽고 1~2줄 작성** (`brand_intro.py`).
      숫자·순위·수상 같은 검증 불가 표현이 나오면 자동 폐기 → 카테고리 문구로 폴백.
    - 초안 생성 경로는 두 개, 둘 다 동일 결과: `메일초안_생성.py --brands "A,B"` /
      대시보드 셀러 디테일의 '이 브랜드 메일 초안 만들기' 버튼.
