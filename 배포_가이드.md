# 대시보드 인터넷 배포 가이드 (누구나 URL로 접속)

목표: `https://OOO.streamlit.app` 같은 **공개 주소**를 만들어, 다른 사람에게 전달하면
어디서든(외부 인터넷 포함) 접속하게 하기. **무료**이고, 쓰시는 GitHub을 그대로 이용해요.

> 천천히 따라 하시면 됩니다. 막히면 그 화면을 캡처해서 물어보세요.

---

## 먼저 알아둘 것 (중요)

1. **수집(브랜드 자동수집)은 클라우드에서 안 돌아가요.** 수집은 브라우저가 필요해서
   본인 PC에서 `대시보드_실행.bat` 으로만 하세요. **클라우드 대시보드는 '보기 + 메일 작성' 공유용**이에요.
   (데이터는 Supabase 클라우드 DB라, 본인이 로컬에서 수집하면 클라우드 대시보드에도 똑같이 보여요.)
2. **비밀키는 절대 GitHub에 직접 올리지 마세요.** 이미 `.gitignore`로 `.env`가 보호되고,
   키는 Streamlit Cloud의 **Secrets** 칸에만 넣어요.
3. **접속 비밀번호(APP_PASSWORD)를 꼭 설정**하세요. 공개 URL이라 비번 없으면 아무나 들어와요.

---

## 1단계. GitHub에 최신 코드 올리기 (GitHub Desktop)

1. GitHub Desktop 열기 → 이 프로젝트(`셀픽 영업처 수집`) 선택
2. 왼쪽에 변경된 파일 목록 확인 → **`.env` 가 목록에 없는지 꼭 확인** (있으면 안 됨!)
   - `.env` 가 보이면 멈추고 알려주세요. (`.gitignore` 가 막아주지만 혹시 모르니 확인)
3. 아래 Summary에 메시지 입력 (예: `클라우드 배포 준비`) → **Commit to main**
4. 우측 상단 **Push origin** 클릭

## 2단계. Streamlit Cloud 접속

1. https://share.streamlit.io 접속
2. **Continue with GitHub** → GitHub 계정으로 로그인 → 권한 허용

## 3단계. 앱 배포

1. **Create app** (또는 New app) 클릭 → **Deploy a public app from GitHub**
2. 입력:
   - Repository: 이 프로젝트 저장소 선택
   - Branch: `main`
   - Main file path: `dashboard.py`
3. **Advanced settings** 클릭 → **Secrets** 칸에 아래를 붙여넣고 **실제 값**으로 바꾸기:
   ```toml
   SUPABASE_URL = "본인 supabase url"
   SUPABASE_KEY = "본인 supabase key"
   APP_PASSWORD = "원하는 접속 비밀번호"
   ```
   (SUPABASE_URL / KEY 는 본인 `.env` 파일에 있는 값과 똑같이 넣으면 돼요)
4. **Deploy** 클릭 → 2~3분 기다리면 빌드 완료

## 4단계. 완성 · 공유

- 배포되면 `https://OOO.streamlit.app` 주소가 생겨요.
- 그 **주소 + 접속 비밀번호**를 다른 사람에게 전달하면, 어디서든 접속 → 비번 입력 → 대시보드 사용.

---

## 자주 묻는 것

- **첫 접속이 느려요 / "zzz" 화면**: 무료 플랜은 한동안 안 쓰면 잠들어요. 첫 접속 시 깨어나며 30초쯤 걸려요. 정상입니다.
- **비밀번호 화면이 안 떠요**: Secrets에 `APP_PASSWORD` 를 안 넣은 거예요. 넣으면 로그인 화면이 생겨요.
- **데이터가 안 보여요**: Secrets의 SUPABASE_URL / KEY 가 틀렸을 가능성. 본인 `.env` 값과 같은지 확인.
- **코드를 또 고치면?**: GitHub Desktop에서 Commit → Push 하면 클라우드가 자동으로 다시 배포해요.
- **수집 버튼을 눌렀더니 안 돼요**: 정상이에요. 수집은 본인 PC(.bat)에서만. 클라우드는 보기·메일용.
