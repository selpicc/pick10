# 셀픽 영업처 수집 — 작업 매뉴얼 (Claude Code용)

이 프로젝트는 영유아·임산부 브랜드를 자동 수집해 영업 리드를 만들고, 브랜드별 맞춤
영업메일 초안을 Gmail에 생성하는 도구입니다. 사용자(밍)는 개발 초보이므로 **항상
쉽게 설명**하고, **진행 전에 물어보고**, **오류는 보고하고 같은 오류가 반복되지 않게**
처리하세요. 코드는 조각이 아니라 전체 흐름을 보고 수정하세요.

## 환경
- OS: Windows. 파이썬은 항상 가상환경으로 실행: `venv\Scripts\python ...`
- 비밀키: `.env` (NAVER_CLIENT_ID/SECRET, SUPABASE_URL/KEY, GOOGLE_API_KEY 등) — 절대 커밋 X
- DB: Supabase `sellers` 테이블. 대시보드는 여기서 읽어 보여줌 (수집하면 자동 반영)
- 데이터 흐름: 수집(collect_5.py) → Supabase 저장 → 대시보드 표시 → 메일 초안(메일초안_생성.py)

## 자주 하는 일 — 명령 매핑

### 1) 브랜드 수집
- 키워드로:   `venv\Scripts\python collect_5.py --keywords "튼살크림" --count 3`
- 카테고리로: `venv\Scripts\python collect_5.py --category "베이비 스킨케어" --count 5`
- 자동(전체): `venv\Scripts\python collect_5.py --count 5`
- 카테고리 종류: 분유·이유식 / 임산부 영양제 / 기저귀·물티슈 / 출산 서비스 /
  임산부 뷰티 / 수유용품 / 이동·외출 / 임산부 의류 / 유아 의류 / 베이비 스킨케어
- 수집은 Supabase에 자동 저장됨 → 대시보드에 바로 반영됨 (별도 작업 불필요)
- 콤마로 여러 키워드 가능: `--keywords "산양분유, 유기농 기저귀"`
- ⭐ 창 꺼짐 대비(권장): 명령창이 죽어도 안 끊기는 백그라운드 런처가 있음.
  `venv\Scripts\python 수집_백그라운드.py --keywords "..." --count 3`
  → collect_5.py를 분리(detached) 실행. 부모 창이 죽어도 수집은 끝까지 돌고 저장됨.
  진행은 `수집로그_최근.log`, 상태는 `수집상태.json`에 기록. 다시 켜면
  `venv\Scripts\python 수집_상태확인.py` 로 끝났는지/결과를 확인하고 이어서 메일 진행.

### 2) 메일 초안 생성 (Gmail 임시보관함)
- 이메일 있는 신규 브랜드 전부: `venv\Scripts\python 메일초안_생성.py`
- 특정 브랜드만:             `venv\Scripts\python 메일초안_생성.py --brand 엘빈즈`
- 방금 수집한 N개만:         `venv\Scripts\python 메일초안_생성.py --brands "엘빈즈,비쥬앤허그"`
  (콤마 구분. "수집+메일" 명령일 땐 반드시 이걸로 그 N개만 한정 — 전체 대상 X)
- 발송은 안 함(초안만). 회사소개서 첨부·전송은 사람이 Gmail에서 직접.
- 전제: Gmail 1회 연동 필요(`token.json`). 없으면 안내하고 `Gmail_연동_설정가이드.md`로 유도.
- 대시보드에서도 가능: 브랜드 행 클릭 → 셀러 디테일의 "이 브랜드 메일 초안 만들기".
  (단 **로컬 대시보드만** — 클라우드엔 Gmail 열쇠가 없어 버튼이 안 보인다)

### 2-1) 메일 발송·회신 추적 / 팔로업
- 현황 보기:   `venv\Scripts\python 메일_추적.py`
- 팔로업 초안: `venv\Scripts\python 메일_추적.py --followup`  (7일 무응답 · 최대 2회)
- 매일 09:30·14:00·17:00 하루 3번 자동 실행 중 (윈도우 작업 스케줄러 `셀픽_메일자동추적_1~3`).
  등록/해제/확인: `venv\Scripts\python 메일_자동추적_등록.py [--time "09:30,14:00,17:00"|--remove|--status]`
  (--time 에 콤마로 여러 시각 → 그 개수만큼 작업 등록. 재실행 시 기존 작업은 자동 정리)
- 발송·회신을 감지하면 **영업 상태를 자동 갱신** (발송→'메일 발송', 회신→'컨택중').
  단 사람이 정한 값(계약 완료·거절·패싱·컨택중)은 절대 덮어쓰지 않음.
- 대시보드 '메일' 칸: (빈칸)/📝 초안/발송 N일/🔔 팔로업 때/💬 회신

### 3) 수집 품질 진단/측정 (느리거나 안 잡힐 때)
- `venv\Scripts\python 진단_수집측정.py` → 브랜드별 시간 + 전화/이메일 수집 여부 + `진단_로그.txt`
- 키워드 0건이면 `collect_5.py --keywords "..."`를 직접 돌려 단계별 로그(잡몰컷/키워드매칭)를 봄

### 4) 코드 점검 (수정 후 항상)
- `venv\Scripts\python self_check.py` → 8~9개 항목 PASS/FAIL. 코드 고친 뒤 꼭 돌릴 것.

### 5) 대시보드 실행 / DB 초기화
- 대시보드: `venv\Scripts\streamlit run dashboard.py` → http://localhost:8501
- DB 초기화(백업 후 전체삭제): `venv\Scripts\python reset_db.py` (RESET 입력 필요)

## 자연어 명령 → 실행 레시피 (예시)
- "튼살크림 3개 수집하고 메일 초안 만들어줘"
  1. `collect_5.py --keywords "튼살크림" --count 3` 실행 (수집·DB저장·대시보드반영 자동)
  2. 결과(선별 N건/브랜드명) 요약 보고 — 이때 수집된 브랜드명을 기억
  3. `메일초안_생성.py --brands "방금수집한브랜드명들(콤마구분)"` 실행
     → **그 N개에만** Gmail 초안 생성 (전체 대상 아님!). 그냥 `메일초안_생성.py` 단독 실행 금지
  4. 생성된 초안 수 + "Gmail 임시보관함에서 첨부 후 발송" 안내
- "신생아 로션 5개만 수집해줘" → 1번만
- "방금 수집한 거 메일 초안 만들어줘" → 2단계만 (메일초안_생성.py)
- "코스테일러 전화 왜 안 잡혀?" → 공식몰 직접 확인 후 원인 분석·수정, self_check로 검증

## 영업메일 원칙 (2026-07 개편 — 문구 고칠 때 반드시 지킬 것)
- 근거 수치는 **셀픽 MEDIA PROPOSAL(2026.07)에 실제로 있는 값만**: 조리원 230곳(M/S 50%),
  산부인과 55곳, 체류 14일 매일 접촉, 1인 15장 무료인화, 키오스크 월 300만 노출 보장,
  매월 신규 산모 DB 5,100건. **성과 수치(전환율 등)는 소개서에 없으므로 절대 지어내지 말 것.**
- 레퍼런스(네슬레 일루맘클럽·리안 드림콧·퓨어락)는 **과거형**으로. 현재 집행 중 아님.
- **가격은 첫 메일·팔로업 모두 미노출.** 금액은 거절 트리거 → 견적은 회신·미팅에서.
- 제목에 회사명·'제안' 안 씀. 첫 문단은 우리 소개가 아니라 상대 브랜드 얘기.
- CTA는 하나: 소개서 첨부 + 유선/미팅 요청("편하신 시간만 한 줄 회신").
- 서비스형은 지역형↔전국형 자동 분기(`_detect_region`). 근거 없으면 **지역명을 지어내지 말고** 전국형.
- 도입부는 AI(Gemini)가 홈페이지를 읽고 1~2줄 작성(`brand_intro.py`). 숫자·순위·수상 등
  검증 불가 표현이 나오면 자동 폐기 → 카테고리 문구로 폴백.
- **수기 입력이 자동 수집을 항상 이긴다** (이메일 등). 사람이 고친 값을 덮어쓰지 말 것.

## 수집 핵심 원칙 (코드 수정 시 반드시 지킬 것)
- 연락처·이메일은 **그 브랜드 공식 홈페이지에 표기된 것만** 수집. 없으면 빈칸(수기 입력 필요).
  비공식 사이트(뉴스/마켓/허브몰)에서 가져오지 말 것.
- 만물상/종합몰(타깃 집중도 낮음)은 제외하되, 키워드 매칭은 검색 확장 키워드(구절)까지 인정
  하고 단독 generic 토큰(산모/크림)은 제외(false positive 방지).
- 정확도와 속도 트레이드오프를 바꿀 땐 `진단_수집측정.py`로 측정 후 변경. 추측으로 엔진 건드리지 말 것.

## 명령창이 꺼질 때 (conhost 충돌)
- 증상: 명령 진행 중 검은 명령창이 통째로 닫힘 → 윈도우 기본 콘솔(conhost.exe)이
  0xc0000409로 충돌하는 OS 버그. 수집처럼 출력이 빠르게 쏟아질 때 잘 터짐.
- 근본 해결: **Windows Terminal에서 Claude Code 실행**(2026-06-28 설치 완료, conhost 미사용).
- 데이터 보호: 수집은 위 `수집_백그라운드.py`로 돌리면 창이 죽어도 안 끊김.
- 재발 점검: `Get-WinEvent -FilterHashtable @{LogName='Application';Level=2} | ? {$_.Message -match 'conhost'}`

## 주의
- 커밋/푸시는 사용자 확인 후. 커밋 문구는 제안해서 전달.
- 메일은 **초안까지만** 자동화. 발송 버튼은 사람이.
- 워크스페이스(샌드박스)가 아니라 사용자 PC에서 실행됨 — 실제 수집·브라우저 사용 가능.
