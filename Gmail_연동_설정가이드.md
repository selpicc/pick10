# Gmail 자동 초안 — 최초 1회 설정 가이드

대시보드 버튼으로 영업메일 초안을 자동 생성하려면, **딱 한 번** 구글 인증을 연결해야 해요.
한 번 해두면 그 뒤로는 버튼만 누르면 됩니다. 천천히 따라 하시면 됩니다.

> 발송은 자동으로 하지 않아요. **초안까지만** 자동 생성하고, 첨부파일과 보내기는 직접 하세요.

---

## 0단계. 필요한 라이브러리 설치 (명령창에서 1회)

```
venv\Scripts\pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib
```

---

## 1단계. 구글 클라우드 프로젝트 만들기

1. https://console.cloud.google.com/ 접속 (ondayon2@gmail.com 으로 로그인)
2. 상단 프로젝트 선택 → **새 프로젝트** → 이름 `selpic-mail` → 만들기

## 2단계. Gmail API 켜기

1. 왼쪽 메뉴 → **API 및 서비스 → 라이브러리**
2. `Gmail API` 검색 → 클릭 → **사용 설정**

## 3단계. OAuth 동의 화면 설정

1. **API 및 서비스 → OAuth 동의 화면**
2. User Type: **외부** 선택 → 만들기
3. 앱 이름 `selpic-mail`, 사용자 지원 이메일 = 본인 메일, 개발자 연락처 = 본인 메일 → 저장
4. **테스트 사용자** 단계에서 **+ ADD USERS** → `ondayon2@gmail.com` 추가 → 저장
   (이걸 해야 본인 계정으로 인증이 됩니다)

## 4단계. OAuth 클라이언트(데스크톱 앱) 만들기

1. **API 및 서비스 → 사용자 인증 정보**
2. **+ 사용자 인증 정보 만들기 → OAuth 클라이언트 ID**
3. 애플리케이션 유형: **데스크톱 앱** → 이름 아무거나 → 만들기
4. 팝업에서 **JSON 다운로드** 클릭
5. 다운로드한 파일 이름을 **`credentials.json`** 으로 바꿔서
   이 프로젝트 폴더(`셀픽 영업처 수집`)에 넣기

## 5단계. 최초 인증 (명령창에서 1회)

```
venv\Scripts\python auth_gmail.py
```

- 브라우저가 열립니다 → ondayon2@gmail.com 로그인
- "Google에서 확인하지 않은 앱" 경고가 뜨면 → **고급 → (안전하지 않음) 이동** 클릭
  (내가 만든 내 앱이라 안전합니다)
- 권한 허용 → 완료되면 폴더에 **`token.json`** 이 생깁니다

✅ 여기까지 하면 끝! 이제 대시보드에서 **"영업메일 초안 일괄 생성"** 버튼을 쓸 수 있어요.

---

## 자주 묻는 것

- **token.json 이 뭐예요?** 인증 열쇠 파일이에요. 생기면 다시 로그인 안 해도 됩니다.
- **credentials.json / token.json 을 공유해도 되나요?** 아니요. 내 계정 열쇠라 비밀로 두세요.
  (실수로 깃허브에 올리지 않게 .gitignore 에 넣어두는 걸 권장)
- **나중에 다른 PC에서도 쓰려면?** credentials.json 으로 다시 5단계만 하면 됩니다.
